{
  "description": "An annotations output format result containing validity alongside collected annotations.",

  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "tag:bowtie.report,2026:output:annotations",

  "type": "object",
  "required": ["valid", "annotations"],
  "properties": {
    "valid": {
      "description": "Whether the instance validated successfully against the schema.",
      "type": "boolean"
    },
    "annotations": {
      "description": "Annotation results collected during evaluation. Each item represents a single annotation produced by a keyword.",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "keyword": {
            "description": "The keyword that produced this annotation.",
            "type": "string"
          },
          "instanceLocation": {
            "description": "The location of the instance being annotated, as a plain JSON Pointer (e.g. '' or '/foo').",
            "type": "string"
          },
          "keywordLocation": {
            "description": "The location within the schema of the keyword that produced this annotation, as a URI fragment JSON Pointer (e.g. '#/properties/foo/title').",
            "type": "string"
          },
          "annotation": {
            "description": "The annotation value produced by the keyword."
          }
        },
        "required": ["keyword", "instanceLocation", "keywordLocation", "annotation"],
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
